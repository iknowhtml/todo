{
  "name": "todo-application",
  "version": "0.0.3",
  "workspaces": [
    "client/*",
    "server/*"
  ],
  "scripts": {
    "start": "skaffold dev",
    "lerna": "lerna",
    "version": "lerna version --conventional-commits --yes",
    "tag": "lerna run tag",
    "package": "lerna run package --since $PREVIOUS_TAG",
    "push": "lerna run push --since $PREVIOUS_TAG",
    "update": "helm dependency update infrastructure/helm",
    "deploy": "helm upgrade todo --namespace todo infrastructure/helm --set server.graphql.deployment.image.tag=$TODO_GRAPHQL_TAG --set environment.production=true --set environment.development=false",
    "cleanup": "helm delete todo && kubectl delete pvc data-todo-postgresql-0"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}