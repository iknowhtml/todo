version: '3'
services:
  nginx: 
    image: nginx:latest
    volumes: 
      - ./server/nginx/nginx.conf:/etc/nginx/nginx.conf
    ports: 
      - 80:80
    depends_on:
      - graphql
    
  graphql:
    image: iknowhtml/todo-graphql:latest
    build:
      context: ./server/graphql
    working_dir: /usr/app
    ports: 
      - 9090:9090